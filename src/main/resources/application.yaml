server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shopcart
    username: root
    password: Bestv001!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.example.shopcart,model
logging:
  file: startup.log
